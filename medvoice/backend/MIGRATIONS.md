# MedVoice Database Migrations (Alembic)
# Run: alembic init migrations && alembic revision --autogenerate -m "initial"
# Then: alembic upgrade head

# In production, DB is auto-created via SQLAlchemy's create_all on startup.
# Alembic is used for production schema migrations.
